services:
  - type: web
    name: migracao-ricardo
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run p2.py --server.port $PORT --server.address 0.0.0.0
    envVars:
      - key: PORT
        value: 10000